import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fastandcurious.app',
  appName: 'Fast&Curious',
  webDir: 'dist/fast-and-curious/browser'
};

export default config;
